export interface DynamicButtonPageSelectors {
    dynamicButton: string;
    dynamicButtonText: string;
}

export const dynamicButtonPageSelectors: DynamicButtonPageSelectors = {
    dynamicButton: '.btn.btn-primary',
    dynamicButtonText: 'Button with Dynamic ID',
};